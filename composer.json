{
  "name": "mantelimustikka-prog/multisite-network-email-manager",
  "description": "WordPress multisite network email manager scaffold",
  "type": "project",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=7.4"
  },
  "autoload": {
    "classmap": [
      "includes/",
      "admin/"
    ]
  },
  "scripts": {
    "lint": [
      "php -l multisite-network-email-manager.php",
      "find includes admin tests -name '*.php' -print0 | xargs -0 -n1 php -l"
    ],
    "test": "php tests/run.php"
  },
  "config": {
    "sort-packages": true
  }
}
